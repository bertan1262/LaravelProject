/* 
 * Dynamic Theme Controller 
 * Handles dynamic application of theme classes 
 */
const ThemeController = {
    applyTheme1: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-1';
            console.log('Successfully applied theme 1 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme2: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-2';
            console.log('Successfully applied theme 2 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme3: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-3';
            console.log('Successfully applied theme 3 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme4: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-4';
            console.log('Successfully applied theme 4 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme5: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-5';
            console.log('Successfully applied theme 5 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme6: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-6';
            console.log('Successfully applied theme 6 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme7: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-7';
            console.log('Successfully applied theme 7 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme8: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-8';
            console.log('Successfully applied theme 8 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme9: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-9';
            console.log('Successfully applied theme 9 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme10: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-10';
            console.log('Successfully applied theme 10 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme11: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-11';
            console.log('Successfully applied theme 11 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme12: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-12';
            console.log('Successfully applied theme 12 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme13: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-13';
            console.log('Successfully applied theme 13 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme14: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-14';
            console.log('Successfully applied theme 14 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme15: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-15';
            console.log('Successfully applied theme 15 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme16: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-16';
            console.log('Successfully applied theme 16 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme17: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-17';
            console.log('Successfully applied theme 17 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme18: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-18';
            console.log('Successfully applied theme 18 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme19: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-19';
            console.log('Successfully applied theme 19 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme20: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-20';
            console.log('Successfully applied theme 20 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme21: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-21';
            console.log('Successfully applied theme 21 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme22: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-22';
            console.log('Successfully applied theme 22 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme23: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-23';
            console.log('Successfully applied theme 23 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme24: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-24';
            console.log('Successfully applied theme 24 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme25: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-25';
            console.log('Successfully applied theme 25 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme26: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-26';
            console.log('Successfully applied theme 26 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme27: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-27';
            console.log('Successfully applied theme 27 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme28: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-28';
            console.log('Successfully applied theme 28 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme29: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-29';
            console.log('Successfully applied theme 29 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme30: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-30';
            console.log('Successfully applied theme 30 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme31: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-31';
            console.log('Successfully applied theme 31 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme32: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-32';
            console.log('Successfully applied theme 32 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme33: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-33';
            console.log('Successfully applied theme 33 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme34: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-34';
            console.log('Successfully applied theme 34 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme35: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-35';
            console.log('Successfully applied theme 35 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme36: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-36';
            console.log('Successfully applied theme 36 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme37: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-37';
            console.log('Successfully applied theme 37 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme38: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-38';
            console.log('Successfully applied theme 38 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme39: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-39';
            console.log('Successfully applied theme 39 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme40: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-40';
            console.log('Successfully applied theme 40 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme41: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-41';
            console.log('Successfully applied theme 41 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme42: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-42';
            console.log('Successfully applied theme 42 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme43: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-43';
            console.log('Successfully applied theme 43 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme44: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-44';
            console.log('Successfully applied theme 44 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme45: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-45';
            console.log('Successfully applied theme 45 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme46: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-46';
            console.log('Successfully applied theme 46 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme47: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-47';
            console.log('Successfully applied theme 47 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme48: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-48';
            console.log('Successfully applied theme 48 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme49: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-49';
            console.log('Successfully applied theme 49 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme50: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-50';
            console.log('Successfully applied theme 50 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme51: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-51';
            console.log('Successfully applied theme 51 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme52: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-52';
            console.log('Successfully applied theme 52 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme53: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-53';
            console.log('Successfully applied theme 53 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme54: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-54';
            console.log('Successfully applied theme 54 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme55: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-55';
            console.log('Successfully applied theme 55 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme56: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-56';
            console.log('Successfully applied theme 56 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme57: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-57';
            console.log('Successfully applied theme 57 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme58: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-58';
            console.log('Successfully applied theme 58 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme59: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-59';
            console.log('Successfully applied theme 59 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme60: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-60';
            console.log('Successfully applied theme 60 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme61: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-61';
            console.log('Successfully applied theme 61 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme62: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-62';
            console.log('Successfully applied theme 62 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme63: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-63';
            console.log('Successfully applied theme 63 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme64: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-64';
            console.log('Successfully applied theme 64 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme65: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-65';
            console.log('Successfully applied theme 65 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme66: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-66';
            console.log('Successfully applied theme 66 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme67: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-67';
            console.log('Successfully applied theme 67 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme68: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-68';
            console.log('Successfully applied theme 68 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme69: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-69';
            console.log('Successfully applied theme 69 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme70: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-70';
            console.log('Successfully applied theme 70 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme71: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-71';
            console.log('Successfully applied theme 71 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme72: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-72';
            console.log('Successfully applied theme 72 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme73: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-73';
            console.log('Successfully applied theme 73 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme74: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-74';
            console.log('Successfully applied theme 74 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme75: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-75';
            console.log('Successfully applied theme 75 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme76: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-76';
            console.log('Successfully applied theme 76 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme77: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-77';
            console.log('Successfully applied theme 77 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme78: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-78';
            console.log('Successfully applied theme 78 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme79: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-79';
            console.log('Successfully applied theme 79 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme80: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-80';
            console.log('Successfully applied theme 80 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme81: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-81';
            console.log('Successfully applied theme 81 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme82: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-82';
            console.log('Successfully applied theme 82 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme83: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-83';
            console.log('Successfully applied theme 83 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme84: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-84';
            console.log('Successfully applied theme 84 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme85: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-85';
            console.log('Successfully applied theme 85 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme86: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-86';
            console.log('Successfully applied theme 86 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme87: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-87';
            console.log('Successfully applied theme 87 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme88: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-88';
            console.log('Successfully applied theme 88 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme89: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-89';
            console.log('Successfully applied theme 89 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme90: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-90';
            console.log('Successfully applied theme 90 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme91: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-91';
            console.log('Successfully applied theme 91 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme92: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-92';
            console.log('Successfully applied theme 92 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme93: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-93';
            console.log('Successfully applied theme 93 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme94: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-94';
            console.log('Successfully applied theme 94 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme95: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-95';
            console.log('Successfully applied theme 95 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme96: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-96';
            console.log('Successfully applied theme 96 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme97: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-97';
            console.log('Successfully applied theme 97 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme98: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-98';
            console.log('Successfully applied theme 98 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme99: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-99';
            console.log('Successfully applied theme 99 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme100: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-100';
            console.log('Successfully applied theme 100 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme101: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-101';
            console.log('Successfully applied theme 101 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme102: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-102';
            console.log('Successfully applied theme 102 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme103: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-103';
            console.log('Successfully applied theme 103 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme104: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-104';
            console.log('Successfully applied theme 104 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme105: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-105';
            console.log('Successfully applied theme 105 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme106: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-106';
            console.log('Successfully applied theme 106 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme107: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-107';
            console.log('Successfully applied theme 107 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme108: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-108';
            console.log('Successfully applied theme 108 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme109: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-109';
            console.log('Successfully applied theme 109 to ' + elementId);
            return true;
        }
        return false;
    },
    applyTheme110: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) {
            el.className = 'custom-theme-110';
            console.log('Successfully applied theme 110 to ' + elementId);
            return true;
        }
        return false;
    },
    resetTheme: function(elementId) {
        let el = document.getElementById(elementId);
        if (el) el.className = '';
    }
};
